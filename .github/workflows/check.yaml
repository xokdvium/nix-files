on: [pull_request, push]
jobs:
  check-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - uses: cachix/install-nix-action@v24
      - run: nix fmt
    # FIXME: Currently fails for whatever reason on the github runner 
    # - run: nix flake check --no-build
